## Installation

- `docker-compose up -d`

## Rebuild

- `docker-compose down` to turn off existing server
- `docker-compose up -d --build` to rebuild server

## Run without docker

- 'npm install'
- 'npm start'

